# Generated by build/split-spec.mjs from the bundled contract.
# Edit this file directly - it is source. Run `npm run build` to re-bundle.
description: |
  `CONTENT_NOT_FOUND` — `regenerate.previousContentId` is unknown, belongs to another tenant, or
  has passed its retention window. Cross-tenant ids resolve as 404, never 403.
content:
  application/json:
    schema:
      $ref: ../../../shared/components/schemas/ErrorResponse.yaml
    example:
      statusCode: 404
      code: LMS404
      success: false
      message: The content being regenerated could not be found. Generate it again from scratch.
      data: null
      meta: null
      error:
        name: CONTENT_NOT_FOUND
        details: Content 9e2a7c50-4d81-4b36-a07f-5c1e9b3d8f24 does not exist, or belongs to another tenant.
