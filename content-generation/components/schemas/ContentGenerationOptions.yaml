# Generated by build/split-spec.mjs from the bundled contract.
# Edit this file directly - it is source. Run `npm run build` to re-bundle.
type: object
description: |
  Optional steering. **Every field has a service-side default**, so screen 1d — which exposes no
  controls — can omit this object entirely. Defined now so adding a controls panel later is not a
  breaking change.
properties:
  depth:
    type: string
    enum:
      - BRIEF
      - STANDARD
      - DEEP
    default: STANDARD
    description: |
      Overall thoroughness. `BRIEF` suits revision material and optional subtopics; `DEEP` writes
      the derivations and edge cases out in full. Moves explanation length, example count and
      teacher-note detail together.
  explanation:
    $ref: ./ContentExplanationOptions.yaml
  examples:
    $ref: ./ContentExampleOptions.yaml
  image:
    $ref: ./ContentImageOptions.yaml
  teacherAids:
    $ref: ./ContentTeacherAidOptions.yaml
  academic:
    $ref: ./ContentAcademicOptions.yaml
  locale:
    $ref: ../../../shared/components/schemas/LocaleOptions.yaml
  sourceFidelity:
    $ref: ../../../shared/components/schemas/SourceFidelityOptions.yaml
