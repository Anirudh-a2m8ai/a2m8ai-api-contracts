# Generated by a one-time migration split of the combined ai-service contract.
# Edit this file directly - it is source. Run `npm run build` to re-bundle shared/components/responses.
description: "`INVALID_REQUEST` — schema or validation failure."
content:
  application/json:
    schema:
      $ref: ../schemas/ErrorResponse.yaml
    example:
      statusCode: 400
      code: LMS400
      success: false
      message: Validation failed.
      data: null
      meta: null
      error:
        name: INVALID_REQUEST
        details: Two fields did not pass validation — see fieldErrors.
        fieldErrors:
          - field: documentIds
            message: must contain at least 1 item
          - field: generation.structure.targetModuleCount
            message: max must be greater than or equal to min
