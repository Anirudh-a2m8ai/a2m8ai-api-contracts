# Generated by a one-time migration split of the combined ai-service contract.
# Edit this file directly - it is source. Run `npm run build` to re-bundle shared/components/responses.
description: |
  - `RATE_LIMITED` — too many concurrent jobs for this tenant. Retryable.
  - `QUOTA_EXCEEDED` — tenant generation quota exhausted for the billing period. Not retryable.
headers:
  Retry-After:
    $ref: ../headers/RetryAfter.yaml
content:
  application/json:
    schema:
      $ref: ../schemas/ErrorResponse.yaml
    examples:
      rateLimited:
        summary: RATE_LIMITED
        value:
          statusCode: 429
          code: LMS429
          success: false
          message: Too many generation jobs in progress. Please retry shortly.
          data: null
          meta: null
          error:
            name: RATE_LIMITED
            details: 5 of 5 concurrent job slots are in use for this tenant.
      quotaExceeded:
        summary: QUOTA_EXCEEDED
        value:
          statusCode: 429
          code: LMS429
          success: false
          message: Your AI generation quota for this period has been used up.
          data: null
          meta: null
          error:
            name: QUOTA_EXCEEDED
            details: 500 of 500 generation jobs for this billing period have been used.
