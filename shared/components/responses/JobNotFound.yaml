# Generated by a one-time migration split of the combined ai-service contract.
# Edit this file directly - it is source. Run `npm run build` to re-bundle shared/components/responses.
description: |
  `JOB_NOT_FOUND` — unknown `jobId`, a job from another tenant, or a terminal job past its
  1-hour retention window.
content:
  application/json:
    schema:
      $ref: ../schemas/ErrorResponse.yaml
    example:
      statusCode: 404
      code: LMS404
      success: false
      message: Generation job not found.
      data: null
      meta: null
      error:
        name: JOB_NOT_FOUND
        details: No job with this id exists for this tenant, or it is more than 1 hour past completion.
