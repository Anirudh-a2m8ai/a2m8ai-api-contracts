# Generated by a one-time migration split of the combined ai-service contract.
# Edit this file directly - it is source. Run `npm run build` to re-bundle shared/components/responses.
description: "`SERVICE_UNAVAILABLE` — AI service draining or a dependency is down. Retryable."
headers:
  Retry-After:
    $ref: ../headers/RetryAfter.yaml
content:
  application/json:
    schema:
      $ref: ../schemas/ErrorResponse.yaml
    example:
      statusCode: 503
      code: LMS503
      success: false
      message: The AI service is temporarily unavailable.
      data: null
      meta: null
      error:
        name: SERVICE_UNAVAILABLE
        details: The service is draining for a deploy, or a dependency it relies on is down.
