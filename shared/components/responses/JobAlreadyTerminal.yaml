# Generated by a one-time migration split of the combined ai-service contract.
# Edit this file directly - it is source. Run `npm run build` to re-bundle shared/components/responses.
description: "`JOB_ALREADY_TERMINAL` — cancel called on a job already SUCCEEDED, FAILED or CANCELLED."
content:
  application/json:
    schema:
      $ref: ../schemas/ErrorResponse.yaml
    example:
      statusCode: 409
      code: LMS409
      success: false
      message: This job has already finished and cannot be cancelled.
      data: null
      meta: null
      error:
        name: JOB_ALREADY_TERMINAL
        details: The job's status is already SUCCEEDED.
