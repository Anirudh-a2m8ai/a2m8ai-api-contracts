# Generated by a one-time migration split of the combined ai-service contract.
# Edit this file directly - it is source. Run `npm run build` to re-bundle shared/components/responses.
description: "`INVALID_API_KEY` — `x-api-key` missing or wrong."
content:
  application/json:
    schema:
      $ref: ../schemas/ErrorResponse.yaml
    example:
      statusCode: 401
      code: LMS401
      success: false
      message: API key is required.
      data: null
      meta: null
      error:
        name: INVALID_API_KEY
        details: No `x-api-key` header was present on this request.
