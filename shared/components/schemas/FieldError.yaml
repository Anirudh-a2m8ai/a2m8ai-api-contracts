# Generated by a one-time migration split of the combined ai-service contract.
# Edit this file directly - it is source. Run `npm run build` to re-bundle shared/components/schemas.
type: object
description: One field's validation failure, as part of an ErrorDetail.fieldErrors list.
required:
  - field
  - message
properties:
  field:
    type: string
    description: Dot-path to the offending field, e.g. `generation.structure.targetModuleCount`.
  message:
    type: string
    description: Human-readable, specific to this field.
