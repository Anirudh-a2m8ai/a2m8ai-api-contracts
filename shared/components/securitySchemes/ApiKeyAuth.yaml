# Generated by a one-time migration split of the combined ai-service contract.
# Edit this file directly - it is source. Run `npm run build` to re-bundle shared/components/securitySchemes.
type: apiKey
in: header
name: x-api-key
description: |
  Shared server-to-server secret. The LMS backend holds it; it is **never** exposed to the browser.
  Mirrors the existing `ApiKeyGuard` convention in college-lms-backend
  (`src/common/guards/api.guard.ts`).
