# Generated by a one-time migration split of the combined ai-service contract.
# Edit this file directly - it is source. Run `npm run build` to re-bundle shared/components/parameters.
name: jobId
in: path
required: true
description: Job identifier returned by the submit endpoint.
schema:
  type: string
  format: uuid
