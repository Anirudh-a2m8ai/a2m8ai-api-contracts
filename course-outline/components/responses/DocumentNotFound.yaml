# Generated by build/split-spec.mjs from the bundled contract.
# Edit this file directly - it is source. Run `npm run build` to re-bundle.
description: |
  `DOCUMENT_NOT_FOUND` — a `documentId` is unknown, or belongs to another tenant.
  Cross-tenant ids resolve as 404, never 403, so ids cannot be probed.
content:
  application/json:
    schema:
      $ref: ../../../shared/components/schemas/ErrorResponse.yaml
    example:
      statusCode: 404
      code: LMS404
      success: false
      message: One or more course materials could not be found.
      data: null
      meta: null
      error:
        name: DOCUMENT_NOT_FOUND
        details: Document f3c6da52-4097-4b3c-d8e5-0c4f7a1b2369 does not exist, or belongs to another tenant.
