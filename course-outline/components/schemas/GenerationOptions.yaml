# Generated by build/split-spec.mjs from the bundled contract.
# Edit this file directly - it is source. Run `npm run build` to re-bundle.
type: object
description: |
  Optional steering. **Every field has a service-side default**, so screen 1b — which exposes no
  controls today — can omit this object entirely. Defined now so adding an "advanced options" panel
  later is not a breaking change.
properties:
  structure:
    $ref: ./StructureOptions.yaml
  academic:
    $ref: ./AcademicOptions.yaml
  outcomes:
    $ref: ./OutcomeOptions.yaml
  locale:
    $ref: ../../../shared/components/schemas/LocaleOptions.yaml
  sourceFidelity:
    $ref: ../../../shared/components/schemas/SourceFidelityOptions.yaml
