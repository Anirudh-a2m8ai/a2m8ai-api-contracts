# Generated by build/split-spec.mjs from the bundled contract.
# Edit this file directly - it is source. Run `npm run build` to re-bundle.
type: object
required:
  - jobId
  - status
properties:
  jobId:
    type: string
    format: uuid
  status:
    type: string
    enum:
      - QUEUED
      - RUNNING
      - SUCCEEDED
      - FAILED
      - CANCELLED
  progress:
    oneOf:
      - $ref: ./JobProgress.yaml
      - type: "null"
    description: "`null` while `QUEUED`."
  pollAfterMs:
    type:
      - integer
      - "null"
    description: "`null` once terminal."
  startedAt:
    type:
      - string
      - "null"
    format: date-time
  completedAt:
    type:
      - string
      - "null"
    format: date-time
  result:
    oneOf:
      - $ref: ./OutlineResult.yaml
      - type: "null"
    description: Populated **only** when `status` is `SUCCEEDED`.
  error:
    oneOf:
      - $ref: ../../../shared/components/schemas/ErrorDetail.yaml
      - type: "null"
    description: Populated **only** when `status` is `FAILED`.
